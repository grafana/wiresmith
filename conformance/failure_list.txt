# Conformance tests expected to fail.
#
# wiresmith does not support: well-known types, recursive messages,
# JSON/text format, proto2, editions. Tests exercising these features are
# listed here so the conformance runner treats them as expected failures.
#
# As wiresmith adds features, entries should be removed from this list.
# The runner will flag any test that unexpectedly passes or fails.
#
# Summary: 5 expected failures
#     3 — message merge with recursive messages (wiresmith does not
#         generate recursive message fields; corecursive data is
#         discarded as unknown fields)
#     2 — unknown field preservation (wiresmith intentionally discards
#         unknown fields for performance; it is designed to work with
#         messages of known schema)

# Message merge with recursive messages — the corecursive field is
# unknown to wiresmith, so it is discarded during unmarshal and
# absent from the merged output.
Required.Proto3.ProtobufInput.RepeatedScalarMessageMerge.ProtobufOutput
Required.Proto3.ProtobufInput.ValidDataMap.STRING.MESSAGE.MergeValue.ProtobufOutput
Required.Proto3.ProtobufInput.ValidDataOneof.MESSAGE.Merge.ProtobufOutput

# Unknown field preservation — wiresmith intentionally drops unknown
# fields instead of preserving them in the output.
Required.Proto3.ProtobufInput.UnknownOrdering.ProtobufOutput
Required.Proto3.ProtobufInput.UnknownVarint.ProtobufOutput
