# Conformance tests expected to fail.
#
# wiresmith does not support: well-known types, recursive messages,
# JSON/text format, proto2, editions. Tests exercising these features are
# listed here so the conformance runner treats them as expected failures.
#
# As wiresmith adds features, entries should be removed from this list.
# The runner will flag any test that unexpectedly passes or fails.
#
# Summary: 2 expected failures
#     1 — empty message presence (value-type structs cannot distinguish
#         "unset" from "set to empty message")
#     1 — map message value merge with recursive messages (wiresmith does
#         not generate recursive message fields, so corecursive data is
#         preserved as unknown bytes but not merged recursively)

# Empty message — an empty nested message (length 0) should be present
# in the output, but wiresmith's value-type structs marshal it as absent.
Required.Proto3.ProtobufInput.ValidDataScalar.MESSAGE[0].ProtobufOutput

# Map message merge — the corecursive field inside the map value is
# unknown to wiresmith, so repeated entries with the same key concatenate
# unknown bytes rather than performing recursive message merge.
Required.Proto3.ProtobufInput.ValidDataMap.STRING.MESSAGE.MergeValue.ProtobufOutput
